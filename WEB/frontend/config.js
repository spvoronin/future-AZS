// Адрес бэкенда FastAPI. Поменяйте, если сервер поднят на другом хосте/порту.
const API_BASE = "http://127.0.0.1:8000";

// Id станции, которая показывается на главном экране "Заправка".
// В реальном приложении его нужно брать из выбора пользователя / геолокации,
// здесь для простоты берём первую станцию из списка (см. main.js).
window.APP_CONFIG = {
  API_BASE,
  DEFAULT_STATION_ID: null // заполнится автоматически в main.js
};
